{
  "name": "todo.tasks.patch",
  "description": "Update a task (generic/complete/reopen/snooze)",
  "inputSchema": {
    "type": "object",
    "properties": {
      "list_id": {"type": "string"},
      "task_id": {"type": "string"},
      "mode": {"type": "string", "enum": ["generic", "complete", "reopen", "snooze"]},
      "patch": {"type": "object"},
      "remind_at_iso": {"type": "string", "format": "date-time"},
      "tz": {"type": "string"}
    },
    "required": ["list_id", "task_id", "mode"],
    "additionalProperties": false
  },
  "outputSchema": {"type": "object"}
}
