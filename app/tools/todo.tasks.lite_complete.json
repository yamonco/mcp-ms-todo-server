{
  "name": "todo.tasks.lite_complete",
  "description": "Complete a task (lite)",
  "inputSchema": {
    "type": "object",
    "properties": {
      "list_id": {"type": "string"},
      "task_id": {"type": "string"}
    },
    "required": ["list_id", "task_id"],
    "additionalProperties": false
  },
  "outputSchema": {"type": "string"}
}
