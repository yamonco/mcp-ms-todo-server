# Azure / Microsoft Graph
TENANT_ID=organizations
# CLIENT_ID is optional. Leave empty to use default first-party public client via device code.
CLIENT_ID=
SCOPES=Tasks.ReadWrite offline_access

# Server
EXECUTOR_MODE=rest        # rest | pwsh
PORT=8080
TZ=Asia/Seoul

# Logging
LOG_LEVEL=INFO
